# features/rate-content

Лайк / дизлайк урока (отдельно на каждый вид) и квиза. Рядом со счётчиком.

Пока счётчик — только голоса этого браузера (localStorage). Когда появится бэкенд, хук подтянет общие `up`/`down` и будет слать голос; ключи (`lesson:slug:view`, `quiz:slug`) те же.
